name: 云服务器CICD
# 监听 main 分支上的 push 事件
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    # 构建环境使用 ubuntu
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 清空目标服务器上的目录内容
      - name: 清空目标服务器该文件夹下内容
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            rm -rf /nwa/nginx/html/index/*

      # 部署静态文件到服务器
      # 排除 .github 文件夹和所有 .md 文档
      - name: 上传静态资源到服务器
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          source: "./index.html,./avatar.webp,./favicon.ico"
          target: "/nwa/nginx/html/index"
          strip_components: 1
