## 2026-09-26 — 改为博客风格的白底黑字视觉

- 需求：将导航首页改成类似博客的白底黑字、高级简洁风格。
- 修改：调整 `assets/styles.css` 为暖白背景、黑色标题、灰阶说明、细边框与轻阴影；隐藏粒子和能量流，移除卡片霓虹渐变，保留响应式网格、头像和机器人入口；更新 `index.html` 样式版本参数。
- 联动：仅影响首页视觉层，无接口、数据库或权限变更。
- 验证：已刷新用户当前 Chrome 中的 `http://localhost:8088/`，确认白底黑字和卡片层次效果；`git diff --check` 通过。
- 未完成：无。

## 2026-09-26 — 统一卡片标题图标风格

- 需求：让卡片标题图标与白底黑字的博客风格保持一致。
- 修改：将彩色 emoji 替换为统一的灰黑色圆形符号标记，调整标题间距和字面排版；更新 `index.html` 样式版本参数。
- 联动：仅影响首页卡片标题展示，无接口、数据库或权限变更。
- 验证：已刷新用户当前 Chrome 中的 `http://localhost:8088/`，确认标题图标已统一为灰黑色圆形符号；`git diff --check` 通过。
- 未完成：无。

## 2026-09-26 — 恢复卡片标题 emoji 图标

- 需求：恢复更直观、具有识别度的彩色 emoji 图标。
- 修改：撤回灰黑色圆形符号，恢复 `index.html` 中原有的 emoji 标题图标，并更新样式版本参数。
- 联动：仅影响首页卡片标题展示，无接口、数据库或权限变更。
- 验证：已刷新用户当前 Chrome 中的 `http://localhost:8088/`，确认 emoji 图标已恢复；`git diff --check` 通过。
- 未完成：无。

## 2026-09-26 — 优化卡片标题 emoji 选择

- 需求：只调整卡片标题 emoji，使图标与入口语义和整体风格更匹配。
- 修改：博客改为 `📝`、文档改为 `📖`、工具改为 `🧰`、后台改为 `⚙️`、AI 知识库改为 `🧠`；CSDN 和 GitHub 保留 `📚`、`🐙`。
- 联动：仅影响首页标题文案展示，无接口、数据库或权限变更。
- 验证：已刷新用户当前 Chrome 中的 `http://localhost:8088/`，确认新 emoji 图标显示正常；`git diff --check` 通过。
- 未完成：无。

## 2026-09-26 — 增加克制的流星背景动效

- 需求：在白底黑字风格中增加偶尔划过的流星背景特效。
- 修改：在 `index.html` 增加三个纯 CSS 流星元素，在 `assets/styles.css` 增加低对比度、低频率的斜向流星动画；移动端减少一条流星，并遵循减少动态效果偏好。
- 联动：仅影响首页背景展示，无接口、数据库或权限变更。
- 验证：已刷新用户当前 Chrome 中的 `http://localhost:8088/`，确认多颗流星可错开并同时划过；`git diff --check` 通过。
- 未完成：无。

## 2026-09-26 — 调整为 1–2 秒一颗的流星群

- 需求：让流星更频繁，并产生多颗同时划过的流星群效果。
- 修改：将流星元素增加到 6 颗，统一为约 7 秒循环并以 0.9 秒间隔错开出现；提高尾迹可见度并延长单次可见区间，确保多颗流星重叠；手机端隐藏其中两颗以控制密度。
- 联动：仅影响首页背景展示，无接口、数据库或权限变更。
- 验证：待刷新本地 `http://localhost:8088/` 观察流星群效果，并执行 `git diff --check`。
- 未完成：无。

## 2026-09-26 — 让页脚与悬浮机器人对齐

- 需求：让桌面端底部备案文字与右下角 RAG 机器人处在同一底部区域。
- 修改：恢复桌面端 `.content` 的剩余高度布局，让页脚自然落到底部；手机端继续使用内容自然高度，避免页脚遮挡卡片。
- 联动：仅影响首页纵向布局，无接口、数据库或权限变更。
- 验证：已在用户当前 Chrome 的 `http://localhost:8088/` 标签页刷新确认，页脚已与右下角机器人处于同一底部区域；`git diff --check` 通过。
- 未完成：无。

## 2026-09-26 — 修复页脚距离内容过远

- 需求：解决浏览器中卡片与底部备案信息之间出现大块空白的问题。
- 修改：调整 `assets/styles.css` 的 `.content`，取消占满剩余视口高度，改为内容自然撑开，让页脚紧跟主体内容；屏幕高度不足时仍可自然滚动。
- 联动：仅影响首页纵向布局，无接口、数据库或权限变更。
- 验证：`git diff --check` 通过；已刷新本地 `http://localhost:8088/`，确认页脚已紧跟卡片内容。
- 未完成：无。

## 2026-09-26 — 调整页脚与卡片的垂直间距

- 需求：在收紧页脚位置后，恢复适度的下方留白，避免页脚贴卡片过近。
- 修改：为 `assets/styles.css` 的 `footer` 增加响应式上边距，按视口高度保留 24–56px 的自然间隔。
- 联动：仅影响首页纵向布局，无接口、数据库或权限变更。
- 验证：已刷新本地 `http://localhost:8088/`，确认页脚与卡片之间恢复适度留白；`git diff --check` 通过。
- 未完成：无。

## 2026-09-25 — 延迟加载 RAG 小组件

- 需求：优先完成首页首屏渲染，再加载 RAG 外部小组件。
- 修改：`assets/main.js` 改为在页面 `load` 完成后通过 `requestIdleCallback` 延迟加载 RAG 脚本，并保留 2 秒超时和不支持该 API 时的定时器兜底。
- 联动：RAG 小组件仍保留，只调整加载时机；本地 CSS、图片和页面结构不变。
- 验证：待执行静态检查、CI 部署和线上响应复查。
- 未完成：无。

## 2026-09-25 — 绕过 CDN 旧资源缓存

- 需求：修复线上仍命中旧 HTML 内容导致的 CSS/JS 加载失败。
- 修改：为本地 `styles.css`、`main.js` 和备案图片增加版本查询参数，强制 Cloudflare 请求最新资源。
- 联动：源站 `assets/` 目录已正确部署；无需改动 Nginx 或手动清理服务器文件。
- 验证：确认 Cloudflare 公网响应曾为 `cf-cache-status: HIT`，部署后将复查带版本参数的资源 MIME 类型。
- 未完成：等待本次 CI 部署完成并复查公网资源。

## 2026-09-25 — 改用显式上传 assets 目录

- 需求：彻底修复线上 CSS/JS 资源路径错误。
- 修改：将 CI 的 `source` 从 `assets/*` glob 改为显式上传 `assets` 目录，避免 `appleboy/scp-action` 在 glob 模式下扁平化目录结构。
- 联动：无。
- 验证：已查阅 scp-action 参数说明，并将在部署后检查线上资源 MIME 类型和内容。
- 未完成：等待本次 CI 部署完成并复查线上资源。

## 2026-09-25 — 修复 CI 上传时丢失 assets 目录层级

- 需求：解决线上样式资源路径仍然失效的问题。
- 修改：将 `.github/workflows/CICD.yml` 的 `strip_components` 从 `1` 改为 `0`，保留 `assets/` 目录层级；此前该参数把资源错误上传到了网站根目录。
- 联动：无。
- 验证：已确认线上根目录资源曾返回正确内容但 `/assets/*` 返回首页 HTML，本次部署后复查目录路径。
- 未完成：等待本次 CI 部署完成并复查线上资源。

## 2026-09-25 — 修复 CI 未上传静态资源目录

- 需求：修复线上首页样式全无的问题。
- 修改：更新 `.github/workflows/CICD.yml`，将 `assets/*` 纳入静态资源上传清单，确保 `styles.css`、`main.js` 和备案图片随首页一起部署。
- 联动：无；线上页面继续保留唯一需要的 RAG 外部脚本。
- 验证：定位到线上 `/assets/styles.css` 返回首页 HTML 的问题，修正 CI 配置后重新触发部署。
- 未完成：等待本次 CI 部署完成并复查线上资源类型。

## 2026-09-25 — 拆分首页 CSS 与 JS

- 需求：减少首页 HTML 拥挤程度，并保留清晰注释。
- 修改：将内联样式拆至 `html/lucky_index/assets/styles.css`；新增 `html/lucky_index/assets/main.js`，集中加载唯一需要的 RAG 外部脚本；`index.html` 仅保留结构和资源引入。
- 联动：外部 RAG 小组件继续保留，备案图片仍使用本地资源。
- 验证：执行静态资源检索、`git diff --check`，并刷新 Chrome 原窗口确认页面显示正常。
- 未完成：无。

## 2026-09-25 — 保留 RAG 外部小组件脚本

- 需求：保留首页唯一需要的外部脚本，其余资源继续保持本地静态加载。
- 修改：恢复 `rag.luckynwa.top/widget.js` 外部脚本；备案图片仍使用 `./assets/gonganbeian.png`，未恢复其他外部资源。
- 联动：RAG 小组件继续提供页面右下角交互能力。
- 验证：确认首页自动加载的外部资源仅剩该脚本，并执行 `git diff --check`。
- 未完成：无。

## 2026-09-25 — 收敛为纯静态首页

- 需求：让导航站尽量不依赖外部资源，保持纯静态和快速加载。
- 修改：将备案图片下载至 `html/lucky_index/assets/gonganbeian.png` 并改为本地引用；移除 RAG 小组件外部脚本，避免首页加载时产生外部运行时请求。
- 联动：站点卡片的外部跳转链接保留，仅在用户主动点击时访问。
- 验证：检查首页自动加载资源和 `git diff --check`。
- 未完成：无。

## 2026-09-25 — 移除首页网站存活检测

- 需求：不再在首页加载时检查各个站点的存活状态。
- 修改：移除 `html/lucky_index/index.html` 中的 `fetch`/`HEAD` 状态检测脚本、检测用 `data-url` 属性和状态边框样式。
- 联动：无；保留站点卡片点击链接和现有 RAG 小组件。
- 验证：静态检索确认首页不再包含 `fetch`、`data-url` 或状态检测逻辑；执行 `git diff --check`。
- 未完成：无。
## 2026-09-26 — 首页适配手机与平板响应式布局

- 需求：让 lucky-index 在手机、平板和桌面设备上都能正常浏览和点击。
- 修改：重构 `assets/styles.css` 的布局断点与尺寸系统；桌面使用 4 列、平板使用 3/2 列、手机使用 1 列；移除固定三行和全局隐藏滚动；增加安全区留白、触控友好间距、键盘焦点及减少动态效果支持；更新 `index.html` 的资源版本参数并同步更新 `README.md`。
- 联动：仅影响静态首页展示层，无接口、数据库或权限变更。
- 验证：`git diff --check` 通过；确认 1024px/700px/480px 三组断点；在 Chrome 原窗口加载本地页面确认桌面布局正常，随后恢复线上首页。
- 未完成：无。
